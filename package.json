{
  "name": "webupload",
  "version": "0.1.0",
  "description": "一个简单无依赖的图片上传组件",
  "main": "index.js",
  "scripts": {
    "build": "webpack --watch --progress --colors",
    "server": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "admin",
  "license": "ISC",
  "devDependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "multer": "^1.2.0"
  }
}
